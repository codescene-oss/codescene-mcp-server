# Changelog

## 0.1.0 (Unreleased)

- Initial release
- Bundles CodeScene MCP Server binary per platform
- Automatic MCP server registration in VS Code agent mode
- Settings UI for access token, server URL, and project ID
- Commands for configuration, restart, and status
- Platform-specific extension packaging (macOS, Linux, Windows)
